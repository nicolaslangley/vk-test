#### README

In order for this to work on MacOS the following steps must be performed:
- Download VulkanSDK from: https://vulkan.lunarg.com/sdk/home
- Copy `vulkan.framework` to `/Library/Frameworks` and rename to `Vulkan.framework`
`brew install --static glfw3`
